%h1
  Rooms#Show : #{@room.room_name}
%p
  %span email:
  = @user.email
- @videos.each do |video|
  %p
    %span 動画 :
    = link_to video.name, video_path(video.id)
%p= link_to "動画を貼る", new_video_path
